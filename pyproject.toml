[project]
name = "nabla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Max", email = "maxmueller1611@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.1",
]

[build-system]
requires = ["uv_build>=0.11.15,<0.12.0"]
build-backend = "uv_build"
